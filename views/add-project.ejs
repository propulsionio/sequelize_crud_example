<!DOCTYPE html>
<html>
  <head>
    <title>Project Management</title>
    <link rel='stylesheet' href='//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css'/>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <div class="col-md-offset-3 col-md-6">
        <center><h3>Add Project</h3></center>
        
          <form action="<%if(locals.project){%>/edit_project/<%=project.id%><%}else{%>/projects<%}%>" method="POST">
            <div class="form-group">
              <label for="projectName">Project Name</label>
              <input type="text" class="form-control" id="projectName" name="name" placeholder="Project Name" 
              value="<%if(locals.project){%><%=(locals.project.name || "")%><%}else{%><%=""%><%}%>">
            </div>
            
            <div class="form-group">
              <label for="description">Description</label>
              <input type="text" class="form-control" id="description" name="description" placeholder="Description" value="<%if(locals.project){%><%=(locals.project.description || "")%><%}else{%><%=""%><%}%>">
            </div>
            
            <div class="form-group">
              <label for="totalTeamMembers">Team Size</label>
              <input type="text" class="form-control" name="totalTeamMembers" id="totalTeamMembers" value="<%if(locals.project){%><%=(locals.project.totalTeamMembers || "")%><%}else{%><%=""%><%}%>">
            </div>
            
            <button type="submit" class="btn btn-default">Submit</button>
            
            <%if(locals.project){%>
              <a href="/delete_project/<%=locals.project.id%>" class="btn btn-danger">Delete</a>
            <%}%>
          </form>  
      </div>
    </div> 
    
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"/>
  </body>
</html>
